/**
 * @file
 * junebug_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.junebugTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
